language: csharp

before_script: 
  - mkdir -p .nuget
  - wget -O .nuget/nuget.exe https://dist.nuget.org/win-x86-commandline/latest/nuget.exe  
  - chmod +x build.sh
script:
  - ./build.sh
  - mono .nuget/nuget.exe pack chocolatey/Stratos.nuspec
